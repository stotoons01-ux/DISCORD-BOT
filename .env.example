# Example .env for reminders / MongoDB
# Option A: provide full connection string
# MONGO_URI should be the full atlas connection string. Example:
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.abcd123.mongodb.net/?retryWrites=true&w=majority

# Option B: provide components (the wrapper supports MONGO_URI only, but this shows fields you may use locally)
# MONGO_USER=your_db_user
# MONGO_PASS=your_db_password
# MONGO_HOST=cluster0.abcd123.mongodb.net
# MONGO_DB_NAME=reminder_db
# MONGO_REMINDERS_COLLECTION=reminders

# For local testing you can set a local URI like:
# 

# NOTE: keep this file out of version control if it contains secrets. Use environment variables on production.
# Discord Bot Configuration
DISCORD_TOKEN=your_discord_bot_token_here

# Discord Guild IDs (Server IDs) - Optional but recommended for faster command sync
# You can find these by right-clicking your server name and copying the server ID
GUILD_ID_1
GUILD_ID_2=

# OpenRouter API Configuration
# At least OPENROUTER_API_KEY_1 is required
OPENROUTER_API_KEY_1=your_openrouter_api_key_here
OPENROUTER_API_KEY_2=your_second_api_key_optional
OPENROUTER_API_KEY_3=your_third_api_key_optional

# OpenRouter Model (optional - has default)
OPENROUTER_MODEL=meta-llama/llama-3.1-8b-instruct:free

# Notes:
# - Copy this file to .env and fill in your actual values
# - NEVER commit .env to version control
# - For cloud deployment, set these in your platform's environment variables section
